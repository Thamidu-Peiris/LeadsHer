/**
 * Admin routes that use the standard dashboard chrome (breadcrumbs, theme toggle, avatar)
 * should import this component — it is the same implementation as DashboardTopBar.
 */
export { default } from './DashboardTopBar';
